name = "justjavac/platform"

version = "0.1.2"

readme = "README.mbt.md"

repository = "https://github.com/justjavac/moonbit-platform"

license = "MIT"

keywords = [ "moonbit", "native", "platform", "ffi" ]

description = "Minimal native platform detection for MoonBit packages."

preferred_target = "native"

options(
  source: "src",
)
